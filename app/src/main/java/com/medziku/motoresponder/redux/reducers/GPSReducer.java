public class GPSReducer extends Reducer<GPSState>{
   public GPSState reduce(GPSState state,Action action){
   Class actionType =action.getClass();
  
   if (actionType==GPSStartAction){
      newState
   }
   
   if (actionType==GPSStopAction){
   }
   
   if (actionType==UpdateFromGPSAction){
   
   }
   
   }
}
